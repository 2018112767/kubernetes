
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: podcheckpoints.podcheckpointcontroller.tecent.com
spec:
  group: podcheckpointcontroller.tecent.com
  names:
    kind: PodCheckpoint
    listKind: PodCheckpointList
    plural: podcheckpoints
    singular: podcheckpoint
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodCheckpoint is the Schema for the podcheckpoints API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PodCheckpointSpec defines the desired state of PodCheckpoint
            properties:
              podName:
                type: string
              secretName:
                type: string
              storage:
                type: string
            required:
            - podName
            - secretName
            - storage
            type: object
          status:
            description: PodCheckpointStatus defines the observed state of PodCheckpoint
            properties:
              containerConditions:
                items:
                  properties:
                    checkpointPhase:
                      description: ContainerCheckpointPhase is a label for the condition
                        of a pod at the current time.
                      type: string
                    containerID:
                      type: string
                    containerName:
                      type: string
                  required:
                  - checkpointPhase
                  - containerID
                  - containerName
                  type: object
                type: array
              phase:
                description: PodCheckpointPhase is a label for the condition of a
                  pod at the current time.
                type: string
            required:
            - containerConditions
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
